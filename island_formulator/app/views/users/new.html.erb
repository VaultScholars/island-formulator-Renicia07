<div class="max-w-md mx-auto mt-10 p-6 bg-white rounded-lg shadow-md">
  <h1 class="text-2xl font-bold mb-6 text-center">Create Your Account</h1>

  <%= form_with model: @user, url: users_path, class: "space-y-4" do |form| %>
    <% if @user.errors.any? %>
      <div class="bg-red-200 border border-red-400 text-red-800 px-4 py-3 rounded mb-4">
        <ul class="list-disc list-inside">
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :email_address, class: "block text-sm font-medium text-yellow-950 mb-1" %>
      <%= form.email_field :email_address, class: "w-full px-3 py-2 border border-amber-900 rounded-md focus:outline-none focus:ring-2 focus:ring-amber-900", placeholder: "you@example.com", required: true %>
    </div>

    <div>
      <%= form.label :password, class: "block text-sm font-medium text-yellow-950 mb-1" %>
      <%= form.password_field :password, class: "w-full px-3 py-2 border border-amber-400 rounded-md focus:outline-none focus:ring-2 focus:ring-amber-900", placeholder: "Choose a strong password", required: true %>
    </div>

    <div>
      <%= form.label :password_confirmation, "Confirm Password", class: "block text-sm font-medium text-yellow-950 mb-1" %>
      <%= form.password_field :password_confirmation, class: "w-full px-3 py-2 border border-amber-900 rounded-md focus:outline-none focus:ring-2 focus:ring-amber-900", placeholder: "Type your password again", required: true %>
    </div>

    <%= form.submit "Create Account", class: "w-full bg-lime-800 text-white py-2 px-4 rounded-md hover:bg-yellow-200/50 transition duration-200 font-medium" %>
  <% end %>

  <div class="mt-4 text-center text-sm text-yellow-900">
    Already have an account? 
    <%= link_to "Log in", new_session_path, class: "text-yellow-950 hover:underline" %>
  </div>
</div>