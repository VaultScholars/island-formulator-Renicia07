<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-yellow-50 text-yellow-950 font-medium rounded-lg inline-block mb-5" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Inventory</h1>
    <%= link_to "New Inventory Item", new_inventory_item_path, class: "rounded-lg py-3 px-5 bg-amber-200 text-yellow-950 block font-medium" %>
  </div>

  <div id="inventory_items" class="min-w-full mt-8">
    <div class="overflow-x-auto shadow rounded-lg">
      <table class="min-w-full bg-yellow-100">
        <thead class="bg-yellow-50 border-b">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-orange-950 uppercase tracking-wider">Ingredient</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-orange-950 uppercase tracking-wider">Brand</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-orange-950 uppercase tracking-wider">Size</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-orange-950 uppercase tracking-wider">Location</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-orange-950 uppercase tracking-wider">Purchased</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-orange-950 uppercase tracking-wider">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-yellow-900">
          <% @inventory_items.each do |item| %>
            <tr>
              <td class="px-6 py-4 whitespace-nowrap font-medium text-yellow-950"><%= item.ingredient.name %></td>
              <td class="px-6 py-4 whitespace-nowrap text-orange-950"><%= item.brand %></td>
              <td class="px-6 py-4 whitespace-nowrap text-orange-950"><%= item.size %></td>
              <td class="px-6 py-4 whitespace-nowrap text-orange-950"><%= item.location %></td>
              <td class="px-6 py-4 whitespace-nowrap text-orange-950"><%= item.purchase_date.strftime("%b %d, %Y") %></td>
              <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                <%= link_to "Show", item, class: "text-orange-700 hover:text-orange-400 mr-3" %>
                <%= link_to "Edit", edit_inventory_item_path(item), class: "text-yellow-900 hover:text-taupe-900" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>